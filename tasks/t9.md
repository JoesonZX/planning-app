# 任务：style.css 追加 v9 组件（底部动作表 / misc 折叠 / 日记就地展开）

输出完整重写后的 style.css。**除下述新增与两处指定删除外，其余内容必须逐字节保留**（包括注释、空行、顺序）。

设计语言（必须遵守）：Things 式极简白/深灰；零边框零阴影零渐变（层级靠 --surface 明度差）；圆角 10/14（大容器 18）；字号 12/13/15/17/22；间距 4/8/12/16/24/32；蓝色 --acc 只给交互态。

## 1. 新增段（追加到文件末尾，标题注释 `/* ---------- v9 组件 ---------- */`）

**misc 折叠区**
- `details.misifold`：`margin: 6px 0 0;`
- `details.misifold summary`：字号 12px、色 var(--dim)、`cursor: pointer; padding: 9px 2px;`、去掉默认三角（`list-style: none` + `&::-webkit-details-marker { display: none; }`），前面加一个 CSS `+`／`−` 指示（用 `details[open] summary::before` 切换，色 var(--faint)）
- `details.misifold .cards`：`margin-top: 4px;`
- 折叠区内条目文字降级：`details.misifold .cards .cb { color: var(--dim); font-size: 13.5px; }`

**底部动作表**
- `#sheet-backdrop`：`position: fixed; inset: 0; background: rgba(0,0,0,.35); z-index: 24; opacity: 0; pointer-events: none; transition: opacity .2s;`（暗色模式下 rgba(0,0,0,.55)，用 @media (prefers-color-scheme: dark)）；`.on` 时 `opacity: 1; pointer-events: auto;`
- `#actionsheet`：`position: fixed; left: 50%; bottom: 12px; width: min(420px, 92vw); transform: translate(-50%, 105%); transition: transform .22s ease; background: var(--surface); border-radius: 18px; padding: 6px 10px 8px; z-index: 25;`；`.on` 时 `transform: translate(-50%, 0);`
- `.sheet-title`：14px / weight 650 / var(--fg-strong)，`padding: 10px 8px 2px;`，超两行截断（-webkit-box 2 行）
- `.sheet-sub`：12px / var(--dim)，`padding: 0 8px 6px;`
- `.sheet-btn`：`display: block; width: 100%; text-align: left; background: var(--bg); border: 0; border-radius: 12px; padding: 12px 14px; font-size: 15px; color: var(--fg); cursor: pointer; margin-top: 6px; font-weight: 500;`；`:active { background: var(--surface-2); }`；`.danger { color: var(--bad); }`；`.accent { color: var(--acc); }`
- `.sheet-date`：`display: flex; gap: 8px; margin-top: 6px;`；内 `input[type="date"]`：`flex: 1; background: var(--bg); border: 0; border-radius: 12px; padding: 11px 12px; color: var(--fg); font: inherit;`；内 `.sheet-btn` `width: auto; flex: none;`
- `.sheet-cancel`：`display: block; width: 100%; margin-top: 10px; background: none; border: 0; color: var(--dim); font-size: 13px; padding: 10px 0 6px; cursor: pointer;`

**日记就地展开**
- `.diary-inline .diary-text { min-height: 220px; }`（覆盖全局 52vh——就地展开不该占半屏）
- `.diary-head b`：14px / var(--fg-strong) / weight 650；`.diary-head span`：12px / var(--dim) / `display: block; margin-top: 2px;`
- `.diary-actions`：`display: flex; gap: 8px; margin-top: 10px;`；内 `button.primary { flex: 1; margin-top: 0; width: auto; }`；内 `.mini { margin-top: 0; padding: 11px 16px; font-size: 13px; }`

## 2. 指定删除（只这两处，别的不动）

- `.tmenu, .tconv { … }` 与 `.tconv { … }` 两个规则块（整段删除，按钮已退役）
- `.diary-save { position: sticky; bottom: 12px; }`（日记全屏层退役；保留其他 .diary-save 规则）

验收自查：亮/暗两模式都成立；无新增边框/阴影/渐变；既有选择器一个都不少。
